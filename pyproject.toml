[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "waymo-e2e"
version = "0.1.0"
description = "Waymo Challenge E2E visual planning experiments"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["waymo_e2e*"]
